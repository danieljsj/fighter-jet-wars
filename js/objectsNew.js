
var Entity = {};

	var Building = Object.create(Entity);

	var Projectile = Object.create(Entity);

		var Plane = Object.create(Projectile);

			var Player = Object.create(Plane);

			var Computer = Object.create(Plane);

		var Laser = Object.create(Projectile);


function Entity(){

}

	function Building(){
		this.prototype = 
	}




var Set = {
	players: 	[];
	computers: 	[];
	buildings: 	[];
}

var Team = Object.create(Set);
team.

var world = Object.create(Set);





teams = [
	Object.create(Team),
	Object.create(Team),
];





for( i=0; i<n+=4; i++){
	var entity = Object.create(Player);
	world.entites.push(entity);
	teams[0].entities.push(entity); entity.team = teams[0]
	players[0].entities.push(entity); entity.team = players[0]
}
for( ''; i<n+=2; i++){
	entities[i] = Object.create(NinjaMaster);
}
for( ''; i<n+=1; i++){
	entities[i] = Object.create(jim);
}

