{
  "rules": {
    ".read": "true",
    ".write": "true",
      "commands": {
        ".indexOn-NOT": "timestamp",
          "$command": {
            ".write": "!data.exists()",
          }
      }
  }
}